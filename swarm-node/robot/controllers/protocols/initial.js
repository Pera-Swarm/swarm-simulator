const logger = require('../../../logger/winston');

/**
 * method initial
 * @param none
 */
const initial = () => {
    logger.log('debug', 'robot.controllers.protocols.initial.initial: ');
}

module.exports = {
    initial
}
