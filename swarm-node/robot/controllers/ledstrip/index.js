const logger = require('../../../logger/winston');

/**
 * method lightUpAll
 * @param none
 */
const lightenAll = () => {
    logger.info('robot.controllers.ledstrip.lightenAll: ');
}

module.exports = {
    lightenAll
}
